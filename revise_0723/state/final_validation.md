# Final validation

- Original manuscript was used as the immutable baseline; all edits are under `revise_0723/`.
- Clean source compilation: passed (`main_revised.pdf`, 18 pages).
- Highlight source compilation: passed (`main_revision_highlight.pdf`, 18 pages).
- Bibliography: BibTeX completed using `cja-template.bib` and `IEEEtran.bst`.
- Cross-references: resolved after full reruns.
- New-section layout: major equation overflows were removed; the remaining two 0.94-pt
  overfull warnings originate from unchanged simulation figures.
- Highlight convention: blue revised blocks, black unchanged text.

